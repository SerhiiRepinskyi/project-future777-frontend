import styled from '@emotion/styled';

export const Button = styled.button`
  width: 32px;
  height: 32px;
  margin-right: auto;
  cursor: pointer;
  
  svg {
    fill: rgba(205, 36, 36, 1);
    background: var(--primary-bg-color);
  }
`;
